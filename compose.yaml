services:
  overpass-worker:
    build: .
    env_file:
      - path: .env
        required: false
    volumes:
      - ./config.yaml:/app/config.yaml:ro
      - overpass-output:/app/output
      - overpass-cache:/app/.cache
    restart: unless-stopped

  caddy:
    image: caddy:2
    ports:
      - "2020:2020"
    volumes:
      - overpass-output:/srv/overpass:ro
      - ./deploy/Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy-data:/data
      - caddy-config:/config
    restart: unless-stopped

volumes:
  overpass-output:
  overpass-cache:
  caddy-data:
  caddy-config:
